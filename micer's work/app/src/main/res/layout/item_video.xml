<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/videoItem"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:layout_marginStart="10dp"
    android:layout_marginEnd="10dp"
    android:onClick="VideoItemClick">

    <TextView
        android:id="@+id/this_videoTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/videoTitle"
        android:textSize="25sp"

        app:layout_constraintTop_toTopOf="parent" />
<!--    android:textAppearance="?android:textAppearanceMedium"-->
    <ImageView
        android:id="@+id/videoImage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:src="@drawable/anim_1"
        android:layout_marginTop="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/this_videoTitle"
        android:contentDescription="@string/videoimage" />

</androidx.constraintlayout.widget.ConstraintLayout>